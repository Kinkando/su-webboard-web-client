export enum Order {
    DESC = 'DESC',
    ASC = 'ASC',
}

export enum Field {
    // Search Forum
    CreatedAt = 'createdAt',
    Popular = 'likeCount',
    Ranking = 'ranking',
    Title = 'title',
    Category = 'categoryName',
    AuthorName = 'authorName',
}