export enum Order {
    DESC = 'DESC',
    ASC = 'ASC',
}

export enum Field {
    CreatedAt = 'createdAt',
    Popular = 'likeCount',
    Ranking = 'ranking',
    Title = 'title',
    Category = 'categoryName',
    AuthorName = 'authorName',
}